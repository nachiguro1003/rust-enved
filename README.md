rust-enved embed static files into binary when source code is compiled. 

exec the following command and run example. 
```
cargo run --example reader
```

this tool is under development so do not use in production.
